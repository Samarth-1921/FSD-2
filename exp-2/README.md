In this experiment  we Learned about UI Componets and UI Componets Libraries 
 There are various UI Componets  
       > button
       > TextField
       > Select
       > Rating
       > Checkbox
1. UI componets are are resuable building blocks of a user interface.
2. Each componets handles a specific part of the UI
3. They combine structure (HTML),(CSS)and(JSS)
   These are import because of 
       > Resuability
       > Consistency
       > Easy maintence
UI componets libraries :- These are  pre-built collection of resuable UI Componets 

The common UI componets Libraries are :-

   1. Material UI (MUI) :- Based on google material design offers a large set of pollished react componets with theming support 
   2. Bootstrap  :- One of the oldest libraries.easy to learn and quick responive layout
