In  This expierment:- 
       1. We installed git and done its set-up 
       2. We created to  Exp1  which was about SPA(Single Page Application)
       3. We used various command like git status , git add "exp-1", git commit -m "add exp-1",git push origin
       4. We also installed react-router-dom
       5.We learnt that Vite is use for quicly creating and running a modern forntend project enabling faster development 

Commands used:-
   > git status :- It checks the current state of the files.

   > git add:- It stages selected changes.

   >git commit -m :- It Permanently records those staged changes in the repository with a descriptive message.

   >git push origin:- It uploads the commit to the remote repository.   

React-Router-Dom:-
       It enables navigation in react applications by mapping URLs to componets, allowing seamless, single-page app routing without page reloads

We also learnt about:- 
    1. SPA (Single Page Application)
    2. MPA (Multipage Page Application)      